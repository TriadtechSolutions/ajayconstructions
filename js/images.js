/* =====================================================
   AJAY CONSTRUCTIONS – Image Utility
   Handles fallback or dynamic images if needed.
   Currently using real assets from images folder.
   ===================================================== */

'use strict';

(function initImages() {
    // This script is currently inactive as real assets are being used.
    // Placeholders can be enabled here if needed for missing assets.
})();
